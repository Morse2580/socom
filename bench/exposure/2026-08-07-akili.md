# Exposure run — akili (operator) — 2026-08-07

> Observer's sheet. Protocol and prohibitions: [`README.md`](README.md).
> Row: [[EV-NONAUTHOR-EXPOSURE-01]].
> **Participant is the author, by operator ruling
> [`0006`](../../decisions/0006-the-author-is-the-participant.md).** The
> *no-demo* and *no-doc-fix-first* prohibitions still applied and were not
> broken; *no-favour* and *no-agent* are moot under `0006`.
>
> ⚠️ Sections marked **PENDING** are unfilled because the observer (this
> session) was not present for them — they are the operator's to complete, and
> the run is not closed until they are. Nothing here is inferred: every cell is
> from the terminal transcript the operator pasted.

## 1. Before the session

| | |
|---|---|
| Participant (handle/pseudonym) | `akili` — the author/operator |
| How they were recruited | n/a — operator ruling `0006` |
| **Do they owe me a favour?** | n/a under `0006` |
| Drives AI agents on a real repo? | Yes — this is the operator's normal working mode |
| Platform (macOS / Linux / WSL) | **Linux** *(confirmed — `socom version` → `platform linux`, `python 3.12.3`)* |
| Repo they will use — theirs, real? | `~/projects/test-repos/buzz` — a **test repo**, Rust (Cargo detected), `main`, dirty (`!1 ?11`) |
| **Build under test** — paste `socom version` output | `socom 0.1` · **`build a1cf0802daef`** · `source /home/akili/projects/test-repos/buzz/socom` · `python 3.12.3` · `platform linux` |
| socom commit at run time | **`3aad54a`** — `a1cf0802daef` is the digest of `bin/socom` on `main` as shipped, `cmp` byte-identical to the public `curl` artifact preflighted the same day. **The run was against live shipped code, not a local build.** *(caveat: `socom version` was run after the session, not during; it identifies the binary at that path now. No socom code has shipped since `3aad54a`, so the digest holds.)* |
| Preflight `curl` + `--help` clean? | not run — the operator used an already-installed `socom` |
| Mode | operator solo at the terminal; observer (this session) saw the transcript afterwards, not live |

## 2. (a) STALL POINT — the richest datum

| t+ | What they were trying to do | What stopped them | Verbatim | Read source? | Recovered? |
|---|---|---|---|---|---|
| — | Run the gates socom had just bound | `checks.fast` = `cargo test`, and `cargo` is not installed on the machine | `socom gate fast: RED — checks.fast failed (rc=127)` · `/bin/sh: 1: cargo: not found` | No | No — escalated out of the tool to an agent session |

**Did they abandon the session?** Yes, at `gate fast`. The next action was not
another socom command; it was raising the result for adjudication.

**What they had to read source to understand:** nothing — the failure was legible
on its face. The disagreement was over whether it counted as a defect, not over
what happened.

## 3. (b) DID A BOUND GATE CATCH SOMETHING REAL

| | |
|---|---|
| Did a gate fire on real work? | **No.** It fired on its own binding. |
| What it caught | Nothing. `cargo test` exited 127 — the command never ran. |
| Was the defect pre-existing / organic? | n/a |
| Did they fix it, or bypass it? | Neither — escalated |
| Did they agree it was worth catching? | The operator's position is that RED on rc=127 is **valid behaviour**, not a defect. Observer concurs on the RED; the defect is the `✓` in §4. |

## 4. Free findings — `PILOT.md`'s own report list

| Question | Observed |
|---|---|
| Reached for `git commit --no-verify`? When and why | Not observed |
| A gate fired a FALSE POSITIVE | No — the RED was true |
| A metric misled them (`value` / `cycle` / trust) | **YES.** `✓ bound checks.fast/medium/full → 'cargo test' — gates now run YOUR tests` was printed **without executing `cargo`**, and refuted three commands later by `rc=127`. New instance of [[DEF-STATUS-CLAIMS-UNLABELLED-01]] — see §7. |
| Where discovery stalled *(cross-ref §2)* | `gate fast` |
| They "gamed" a gate | Not observed |

**Anything they said about what they thought socom was FOR:** not recorded.

**Did they hit the adoption surface — 33 planted files, `core.hooksPath`
rewrite — and did they react to it?** They hit it in full: 11 `planted` lines,
15 `wrote` lines, `.gitignore` amended (6 patterns), `core.hooksPath=.githooks`
wired, `.claude/settings.json` statusLine set. **No reaction recorded** — the
operator did not comment on the entry shape. The scout's 2026-08-04 assessment
is therefore neither confirmed nor refuted by this run.

**Observer note — the wedge fix behaved as designed.** `buzz` had a hand-written
`CLAUDE.md`; socom kept it byte-identical, wrote `CLAUDE.socom.md`, and printed
the one `@CLAUDE.socom.md` line for the user to add. Rung read `T1 — compiled
beside your CLAUDE.md, not loaded by it`, `17%`. That is
[[DEF-HANDWRITTEN-CLAUDE-MD-WEDGES-THE-LADDER-01]]'s repair working in the
field, first sighting outside the test suite.

**Observer note — the binary is inside the adopted repo, again, unprompted.**
`socom version` reports `source /home/akili/projects/test-repos/buzz/socom`. The
participant landed the binary in the repo they were adopting, on a second
machine-path and 2 days after the first sighting, without following `PILOT.md`
this time. That is [[DEF-INSTALLED-BINARY-LANDS-INSIDE-THE-ADOPTED-REPO-01]]
reproducing as **the default thing a user does**, not as a doc-induced
mis-step — the row currently attributes it to the doc's ordering. It also shows
up in the `?11` untracked count on the prompt line. **Strengthens the row; does
not change its P1.**

**Observer note — `quickstart` compiles twice.** The full write block appears
twice (`source=0c1bb39ba10c`, then `source=6bbb2d76e822` after the `[checks]`
bind), and `.claude/settings.json` reports `statusLine -> socom statusline` on
pass 1 and `kept your statusLine` on pass 2 — socom deferring to a file it wrote
seconds earlier. Already filed as a Class A surface in
[`0004`](../../decisions/0004-two-boundaries-socom-does-not-represent.md); not a
new row.

## 5. (c) VOLUNTARY SECOND USE — **the metric**

**PENDING — fill on or after 2026-08-14, in a separate sitting.**

| | |
|---|---|
| Date asked | |
| Exact wording used | *"No obligation either way — did you end up running it again?"* |
| **Ran a socom verb again, unprompted?** | |
| If yes — which verb, on what, why | |
| If no — did they say why | |

⚠️ The in-session repeat of `socom compile` (immediately after `quickstart`,
which had already compiled twice) is **not** a second use. Same sitting, and it
was the step the rung meter had just instructed. §5 measures a return after the
session ends.

## 6. Verdict

| | |
|---|---|
| Stall point recorded? | **Yes** — `gate fast`, rc=127 |
| Unstaged gate catch? | **No** |
| **Voluntary second use?** | **PENDING** (2026-08-14) |
| Any prohibition broken? | **No.** Not demoed; no doc was fixed first. *No-favour* and *no-agent* moot under `0006`. |

**Proof tier after this run:** **operator to set.** The `D0`/`D1` definition
lives in `/root/Akili/.claude/skills/root-gate` and is not defined in this repo
— this sheet does not derive it. Two inputs the operator should weigh: the run
is recorded and produced a stall point (which the row asks for), and §5 is not
yet filled (which the row calls the actual metric).

**What this run says the next action is:** fill §5 on 2026-08-14. Nothing in
`buckets/build.md` unblocks before then.

## 7. Rows this run generated

| Finding | Bucket row filed |
|---|---|
| `✓ gates now run YOUR tests` asserted over a command the machine cannot execute — detection *correct* (Cargo.toml → `cargo test`), toolchain *absent*. New sign: prior instances were all wrong-detection. | Appended to [[DEF-STATUS-CLAIMS-UNLABELLED-01]] (READY P1) — not a new row, same class |
| `quickstart` compiles twice; statusLine set then "kept" | None — already covered by `0004` Class A |
| Binary again landed inside the adopted repo (`source /home/akili/.../buzz/socom`), this time **without** following `PILOT.md` — so the cause is not only the doc's ordering | Cross-referenced into [[DEF-INSTALLED-BINARY-LANDS-INSIDE-THE-ADOPTED-REPO-01]] (READY P1) — strengthens it, no new row |
| Wedge repair confirmed in the field | None — evidence, not a defect |
