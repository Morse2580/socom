<?xml version="1.0" encoding="UTF-8"?>
<!--
  SOCOM bench task B-12 — degrade loudly (residue R6: fail-safe default).
  Phase-0 pilot task set (bench/README.md). Fully-auto contract: it asserts the
  tool refuses bad input nonzero rather than fabricating a green.
-->
<promise id="P-BENCH-12" state="open" domain="cli" session="bench">
  <promiser seat="builder" participant="bench:reference"/>
  <promisee seat="orchestrator" participant="human:moses"/>
  <assessors>
    <assessor seat="gate" ref="gate:task-completion"/>
  </assessors>
  <intent>
    <verbatim from="human:moses" date="2026-06-30" embed="true">
      when it can't do something honestly it should say so and stop — never
      fabricate a pass on a missing or broken input
    </verbatim>
    <decoded embed="true">
      The tool fails closed: verifying a nonexistent file, or one that is not
      well-formed XML, exits nonzero with a clear message (residue R6).
    </decoded>
  </intent>
  <contract ref="C-BENCH-12" state="ratified">
    <goal embed="true">
      contract verify exits nonzero on a missing file and on a non-XML file —
      it never fabricates a green.
    </goal>
    <check id="1" assessor="gate:task-completion">
      <run>! ./bin/socom contract verify /no/such/bench/promise.xml</run>
      <expect>exit 0: the negation holds — verify exited nonzero on a missing file</expect>
    </check>
    <check id="2" assessor="gate:task-completion">
      <run>! ./bin/socom contract verify /dev/null</run>
      <expect>exit 0: the negation holds — verify exited nonzero on a non-XML file</expect>
    </check>
    <out-of-scope embed="true">
      Exhaustive bad-input fuzzing; this gates the fail-closed posture on two cases.
    </out-of-scope>
    <regression-surface embed="true">
      A fabricated green on missing/broken input must turn this RED (R6 floor).
    </regression-surface>
  </contract>
</promise>
