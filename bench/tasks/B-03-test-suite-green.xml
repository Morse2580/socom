<?xml version="1.0" encoding="UTF-8"?>
<!--
  SOCOM bench task B-03 — the full smoke suite is green (the behavior invariant).
  Phase-0 pilot task set (bench/README.md). Fully-auto contract.
-->
<promise id="P-BENCH-03" state="open" domain="cli" session="bench">
  <promiser seat="builder" participant="bench:reference"/>
  <promisee seat="orchestrator" participant="human:moses"/>
  <assessors>
    <assessor seat="gate" ref="gate:task-completion"/>
  </assessors>
  <intent>
    <verbatim from="human:moses" date="2026-06-30" embed="true">
      green means green — the whole smoke suite (unit + e2e + xml + ledger) passes
      before anything ships
    </verbatim>
    <decoded embed="true">
      The end-to-end smoke suite passes, and the suite script is itself
      syntactically loadable (it cannot silently no-op).
    </decoded>
  </intent>
  <contract ref="C-BENCH-03" state="ratified">
    <goal embed="true">
      `bash tests/smoke.sh` exits 0 (unit + e2e + xmlcheck + ledgercheck green).
    </goal>
    <check id="1" assessor="gate:task-completion">
      <run>bash tests/smoke.sh</run>
      <expect>exit 0: full smoke suite passes</expect>
    </check>
    <check id="2" assessor="gate:task-completion">
      <run>bash -n tests/smoke.sh</run>
      <expect>exit 0: the suite script parses (no silent no-op via syntax error)</expect>
    </check>
    <out-of-scope embed="true">
      Coverage adequacy of the suite; this task gates that it runs and is green.
    </out-of-scope>
    <regression-surface embed="true">
      Any unit/e2e regression must turn this RED, not be hidden by a skipped run.
    </regression-surface>
  </contract>
</promise>
