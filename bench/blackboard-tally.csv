# socom blackboard trial — the saves tally. Counted BY HAND, deliberately.
#
# Building a saves-counter is scaffolding: if category A never happens, no
# instrumentation would have made it happen. See PILOT.md §the blackboard trial.
#
# LOG THE VERDICT AT CLAIM TIME, BEFORE YOU KNOW HOW THE WORK TURNS OUT.
# Filling it in afterwards turns every session that went well into a "save" —
# the tally then measures mood, not mechanism.
#
# outcome:
#   A  save    — the agent narrowed, abandoned, or changed approach BECAUSE of a finding
#   B  noise   — findings returned, ignored, no effect
#   C  silent  — no findings existed for those paths
#
# findings_returned feeds the secondary metric: findings-per-claim over time.
# A monotonic rise means the blackboard is becoming noise before it becomes
# useful (Hearsay-II's control problem) — watch it as closely as the A count.
#
# Kill criterion: two weeks of concurrent agents with zero A -> STOP.
#
date,session,paths,findings_returned,retracted_returned,outcome,what_changed
2026-08-02,akili-s1-wire,".mcp.json .gitignore",0,0,C,"first claim on a fresh surface — no findings existed yet"
2026-08-02,akili-hook-decommission,".githooks/pre-commit TASKS.md",2,0,B,"both findings verified and both correct, but CONFOUNDED: the session prompt named the task AND the paths, so the findings could only corroborate, not redirect. What they added over the prompt was reasoning (guard passes / 2>/dev/null swallows / venv arms it), not a change of action. Recorded B rather than A deliberately — interesting reasoning is not a save. Methodological note: a prompt that pre-specifies the task cannot test claim-time delivery; future trial sessions must pick work WITHOUT being told which paths carry findings."
